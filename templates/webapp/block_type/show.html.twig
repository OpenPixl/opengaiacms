{% extends 'base.html.twig' %}

{% block title %}BlockType{% endblock %}

{% block body %}
    <h1>BlockType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ block_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ block_type.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ block_type.slug }}</td>
            </tr>
            <tr>
                <th>TypeContent</th>
                <td>{{ block_type.TypeContent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_webapp_block_type_index') }}">back to list</a>

    <a href="{{ path('app_webapp_block_type_edit', {'id': block_type.id}) }}">edit</a>

    {{ include('webapp/block_type/_delete_form.html.twig') }}
{% endblock %}
