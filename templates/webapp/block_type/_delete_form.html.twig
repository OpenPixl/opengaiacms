<form method="post" action="{{ path('app_webapp_block_type_delete', {'id': block_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ block_type.id) }}">
    <button class="btn">Delete</button>
</form>
