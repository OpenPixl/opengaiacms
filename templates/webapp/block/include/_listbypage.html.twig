<table class="table">
    <thead>
        <tr>
            <th></th>
            <th>Nom</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
    {% for block in blocks %}
        <tr>
            <td>{{ block.id }}</td>
            <td>{{ block.name }}</td>
            <td>
                <a href="{{ path('og_webapp_block_show', {'id': block.id}) }}">show</a>
                <a href="{{ path('og_webapp_block_edit', {'id': block.id}) }}" data-bs-toggle="modal" data-bs-target="#modalBlock" data-bs-id="EDIT-Paramètre du block-{{ idpage }}"><i class="fa-duotone fa-cog"></i></a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="4">no records found</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
